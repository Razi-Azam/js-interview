/*
To check whtehre the two 
strings are anagrams of each other
Anagram: Both the strings have same characters
and same frequency of characters.
For example, str1 = allergy, str2 = gerally

act and tac are anagram
*/

function isAnagram(str1, str2) {
    
}